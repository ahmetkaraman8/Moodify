import { StyleSheet } from 'react-native';

export const common = StyleSheet.create({
  allCenteredRow: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
  allCenter: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});
